[Unit]
Description = Mumble Client  
Requires = systemd-user-sessions.service network.target sound.target
After = multi-user.target

[Service]
User = $USER  
Group = $USER 
Type = simple  
ExecStart = /home/$USER/bin/talkiepi
Restart = always 
RestartSec = 5

[Install]
WantedBy = multi-user.target
